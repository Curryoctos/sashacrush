# Prefer editing supabase/functions/.env — that file is what local Edge Functions load.
# This file is kept only as a convenience pointer; secrets here are ignored by the runtime.

# See: supabase/functions/.env.example
